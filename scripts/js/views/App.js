// Generated by CoffeeScript 1.4.0
(function() {

  define(["views/viewport/Viewport", "models/Viewport", "views/toolbar/Toolbar", "models/heightmap/Heightmap", "Alea", "Backbone", "Bootstrap"], function(ViewportView, viewportModel, ToolbarView, heightmapModel) {
    var AppView;
    return AppView = Backbone.View.extend({
      el: document,
      initialize: function() {
        var toolbarView;
        $("[data-toggle=tooltip]").tooltip({
          container: "body"
        });
        toolbarView = new ToolbarView;
        new ViewportView({
          toolbarView: toolbarView
        });
        return console.log("seed", heightmapModel.get("SEED"));
      }
    });
  });

}).call(this);
